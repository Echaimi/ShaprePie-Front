import 'package:flutter/material.dart';

class EventBalanceTab extends StatelessWidget {
  const EventBalanceTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Équilibre Content'),
    );
  }
}
