import 'package:flutter/material.dart';

class EventExpensesTab extends StatelessWidget {
  const EventExpensesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Dépenses Content'),
    );
  }
}
